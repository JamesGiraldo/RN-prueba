import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    view: {
        flex: 1,
        alignItems: 'center',
        marginTop: '50%'
    },
    logo: {
        width: '50%',
        height: 200,
        marginTop: 50,
        marginBottom: 50,
        // borderRadius: 100
    },
    image: {
        width: '100%',
        height: '100%'
    },
});

export default styles;